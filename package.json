{
    "name": "bandofechoes",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "test": "playwright test",
        "test:ci": "playwright test --reporter=line",
        "serve": "http-server -p 3000 -c- .",
        "start": "npm run serve"
    },
    "devDependencies": {
        "@playwright/test": "^1.47.2",
        "http-server": "^14.1.1"
    }
}