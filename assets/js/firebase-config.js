// Replace with your Firebase web app config (Project settings → General → Your apps)
// Example:
// window.FB_CONFIG = {
//   apiKey: "...",
//   authDomain: "your-project.firebaseapp.com",
//   projectId: "your-project-id",
// };

window.FB_CONFIG = window.FB_CONFIG || null; // keep null by default until configured
