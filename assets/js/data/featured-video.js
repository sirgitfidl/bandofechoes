// Featured YouTube video ID (optional fallback).
//
// In production, the featured video is resolved automatically from the first
// item in the Music Videos playlist via the YouTube Data API (see
// assets/js/featured-video-auto.js). If no API key is present, you can set a
// fallback ID here for local/dev.
window.BOE_FEATURED_VIDEO_ID = '';
